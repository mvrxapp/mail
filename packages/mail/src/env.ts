/**
 * Core AECS parsing does not require any Cloudflare bindings. This marker type
 * exists so Worker examples can extend it as hosted-service modules land.
 */
export interface CloudflareMailEnv {}
